apiVersion: batch/v1
kind: Job
metadata:
  name: dart-client-disabledfunnel-tests
  labels:
    app: dart-client-disabledfunnel-tests
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: tests
          image: tests
          env:
            - name: PUBLISHER_HOST
              value: http://testapp-disabledfunnel-svc.default.svc.cluster.local